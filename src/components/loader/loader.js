import React from 'react';
import './loader.scss';

const Loader = () => {
  return (
    <div>
      <h2 className="loader">Please wait, data loading...</h2>
    </div>
  );
};

export default Loader;
